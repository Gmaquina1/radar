
export function SearchBar({query,setQuery,suggestions}:{query:string;setQuery:(v:string)=>void;suggestions:string[]}){return <section id="busca" className="rounded-3xl bg-zinc-950 p-6 text-white shadow-xl md:p-10"><p className="mb-3 text-yellow-300">Busque máquinas, veículos, equipamentos e leilões</p><div className="flex items-center gap-3 rounded-2xl bg-white px-4 py-3 text-zinc-900">⌕<input className="w-full text-lg outline-none" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Digite modelo, cidade, leiloeiro ou categoria" />{query&&<button onClick={()=>setQuery('')} aria-label="Limpar pesquisa">×</button>}</div>{query&&<div className="mt-4 flex flex-wrap gap-2">{suggestions.slice(0,6).map(s=><button key={s} onClick={()=>setQuery(s)} className="rounded-full bg-yellow-400 px-3 py-1 text-sm text-black">{s}</button>)}</div>}</section>}
