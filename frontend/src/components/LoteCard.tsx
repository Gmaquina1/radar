import type { Lote } from '../types/lote.ts'; import { formatDate, getCategory, getCity, getStatus, isValidImageUrl } from '../utils/lotes.ts';
export function LoteCard({lote}:{lote:Lote}){const status=getStatus(lote); const cat=getCategory(lote); return <article className="overflow-hidden rounded-2xl bg-white shadow"><div className="aspect-[16/10] bg-zinc-100">{isValidImageUrl(lote.foto_lote)?<img loading="lazy" src={lote.foto_lote} alt={lote.titulo||'Lote'} className="h-full w-full object-cover"/>:<div className="flex h-full items-center justify-center text-zinc-400">🚜</div>}</div><div className="space-y-3 p-4"><div className="flex gap-2"><span className="rounded-full bg-yellow-400 px-2 py-1 text-xs font-black text-black">{status.toUpperCase()}</span><span className="rounded-full bg-zinc-100 px-2 py-1 text-xs">{cat}</span></div><h3 className="line-clamp-2 min-h-12 font-bold">{lote.titulo||lote.evento||'Lote sem título'}</h3><p className="text-sm text-zinc-600">{lote.leiloeiro}</p><p className="flex items-center gap-2 text-sm">📍{getCity(lote)} {lote.uf}</p><p className="flex items-center gap-2 text-sm">📅{formatDate(lote.data)} {lote.hora}</p><p className="flex items-center gap-2 text-sm">💰{lote.lance_atual||'Valor não informado'}</p><p className="text-sm">Lote: {lote.lote||'não informado'}</p><a className="block rounded-xl bg-zinc-950 p-3 text-center font-bold text-white" href={lote.link_lote||lote.link_evento} target="_blank" rel="noreferrer">Ver lote</a></div></article>}
